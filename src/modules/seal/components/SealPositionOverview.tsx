// import { StatsCard } from '@/modules/ui/components/StatsCard';
// import { t } from '@lingui/macro';

export function SealPositionOverview({ positionIndex }: { positionIndex: number }): React.ReactElement {
  console.log(positionIndex);
  return (
    <div>
      {/* <StatsCard title={t`SKY sealed | MKR`} isLoading={} error={} content={} />
      <StatsCard title={t`USDS borrowed`} isLoading={} error={} content={} />
      <StatsCard title={t`Accumulated rewards`} isLoading={} error={} content={} /> */}
    </div>
  );
}
